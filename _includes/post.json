{
  "title": "{{ item.title | json}}",
  "permalink": "{{ item.permalink | json }}",
  "summary": "{{ item.summary | strip_newlines | json}}",
  "room": "{{ item.room | json}}",
  "presenters": "{{ item.presenters | json}}",
  "track": "{{ item.track | json}}",
  "start": "{{ item.start | json}}",
  "difficulty": "{{ item.difficulty | json}}",
  "day": "{{ item.day | json}}",
  "classes": "difficulty-{{ item.difficulty | replace: ' ','-' | downcase }} track-{{ item.track | replace: ' ','-' | replace: ',','' | downcase }} room-{{ item.room | replace: ' ','-' | downcase }}"
}
