{
  "title": "{{ item.title }}",
  "content": "{{ item.content }}",
  "room": "{{ item.room }}",
  "presenters": "{{ item.presenters }}",
  "track": "{{ item.track }}",
  "start": "{{ item.start }}",
  "difficulty": "{{ item.difficulty }}",
  "day": "{{ item.day }}"
}
